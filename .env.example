# Environment Variables (Copy to .env and update values)

# Secret key for JWT tokens (CHANGE THIS IN PRODUCTION!)
SECRET_KEY=your-secret-key-change-this-in-production-12345

# Database URL (Railway auto-provides this for PostgreSQL)
DATABASE_URL=sqlite:///./cybersecurity_platform.db
# Railway will auto-set: DATABASE_URL=postgresql://...

# Environment (development or production)
ENVIRONMENT=development

# Server settings (Railway auto-sets PORT)
HOST=0.0.0.0
PORT=8000

# CORS origins (comma separated, leave empty to allow all)
CORS_ORIGINS=
# For production: CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Token expiration (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# API Key default expiry (days)
API_KEY_DEFAULT_EXPIRY_DAYS=90
