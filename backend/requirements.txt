# Backend Dependencies for Cybersecurity Platform
# Compatible versions that don't require C++ build tools

# FastAPI - Modern web framework (with compatible pydantic)
fastapi==0.109.0

# Uvicorn - ASGI server with TLS 1.3 support
uvicorn[standard]==0.27.0

# Database
sqlalchemy==2.0.25
alembic==1.13.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pyjwt==2.8.0
bcrypt==4.1.2

# Data validation (compatible with Python 3.13)
pydantic==2.6.0
pydantic-core==2.16.1
email-validator==2.1.0

# CORS and form data
python-multipart==0.0.6

# Additional security
cryptography==42.0.0

# Type hints
typing-extensions>=4.9.0

# For production database (PostgreSQL)
# psycopg2-binary==2.9.9  # Uncomment when using PostgreSQL
